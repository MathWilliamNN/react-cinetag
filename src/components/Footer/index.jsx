import styles from "./Footer.module.css"

const Footer = () => {
    return (
        <footer className={styles.footer}>
            <h2 className={styles.text}> Inspired by Alura </h2>
        </footer>
    )
}

export default Footer